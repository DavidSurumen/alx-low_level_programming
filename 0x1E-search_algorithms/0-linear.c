#include "search_algos.h"
#include <stdio.h>

/**
 * linear_search: Sequencially search the whole list until a match
 *	has been found or the whole list has been searched.
 */
int linear_search(int *array, size_t size, int value)
{


}
